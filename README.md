## This is repo for autopin.it backend

## Set up

After clone this repo, go to this repo, then

    # install all packages
    $ npm install
    # set up git-hooks
    $ ln -s `pwd`/pre-commit .git/hooks/pre-commit
